---
const { className, id } = Astro.props;
---

<section id={id} class={`${className} ${id === 'projects' ? 'min-h-screen pt-20 w-full' : 'min-h-screen py-20 w-full mx-auto lg:w-[740px] pb-24'}`}>
    <slot />
</section>